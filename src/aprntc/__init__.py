"""aprntc — self-improving agents via shadow → distill → promote (codename "Shadow").

Stage 0 surface area is intentionally small: configuration loading and the
BytePlus building blocks (the VikingDB Signature-V4 signer — the gate — and a
thin ModelArk client). Tap collectors, trajectory store, memory, distillation,
and the promotion gate arrive in later stages per the build plan.
"""

__version__ = "0.0.1"
